version: "2"
services:
    services-a:
        build: ./service-a
        ports:
            - "8080:80"
        depends_on:
            - service-b
            - mycache
        labels:
            com.microsoft.acs.dcos.marathon.vhost: "acssampleagents.eastus.cloudapp.azure.com"

    service-b:
        build: ./service-b
        expose:
            - "80"
        labels:
            com.microsoft.acs.dcos.marathon.healthcheck.path: '/'

    mycache:
        image: redis:alpine
        expose:
            - "6379"